import React from 'react';import BreadCrumbs from "../../components/UI/breadCrumbs/BreadCrumbs";import ExitBtn from "../../components/UI/exitBtn/ExitBtn";import Select from "../../components/UI/select/Select";import Button from "../../components/UI/button/Button";import LikeButton from "../../components/UI/likeButton/LikeButton";import SizeList from "../../components/UI/sizeList/SizeList";import Slider from "../../components/Slider/Slider";import Features from "../../components/UI/features/Features";import './ProductPage.module.css'import {useLocation} from "react-router-dom";import {Link} from "react-router-dom";import cl from './ProductPage.module.css'const ProductPage = ({product}) => {    const path = useLocation().pathname    const splitDigits = (price) => price.toString().replace(/\d{1,3}(?=(\d{3})+(?!\d))/g, '$& ')    return (        <div className={cl.product}>            <BreadCrumbs path={path}/>            <header className={cl.product__header}>                <div className={cl.product__top}>                    <h1 className={cl.product__title}>{product.name}</h1>                    <ExitBtn/>                </div>                <span className={cl.product__price}>{splitDigits(product.price)} руб.</span>            </header>            <section className={cl.product__interactive}>                <div className={cl.product__buttons}>                    <Select options={product.sizes} name={'size'}/>                    <div className={cl.bin__buttons}>                        <Button>В корзину</Button>                        <LikeButton/>                    </div>                </div>                <div className={cl.product__sizeList}>                    <SizeList sizes={product.sizes}/>                    <Link to='/product/howtochoose' className={cl.product__link}>Как выбрать размер?</Link>                </div>            </section>            <Slider images={product.images}/>            <div className={cl.product__description}>{product.description}</div>            <Features features={product.features}/>        </div>    );};export default ProductPage;