import React, {useState} from 'react';import cl from './SliderPaginationItem.module.css'const SliderPaginationItem = ({img, currentPicIndex, i}) => {    const [itemClasses, setItemClasses] = useState([cl.item])    return (        <li className={itemClasses.join(' ')}            onMouseEnter={(event) => {                currentPicIndex(i)                setItemClasses([cl.item, cl.active])            }}            onMouseLeave={() => {                setItemClasses([cl.item])            }}        >            <img className={cl.img}                 src={img}                 alt='Фото продукта с другого ракурса.'            />        </li>)};export default SliderPaginationItem;