import React from "react";const SliderThumbnailsItem = ({img, onMouseEnter, i, classes}) => {    return (        <li className={classes} onMouseEnter={() => onMouseEnter(i)}>            {window.innerWidth <= 320 ?                <div>                    <span className='visually-hidden'>{i + 1}</span>                </div>                :                <img                    src={img}                    alt='Фото продукта с другого ракурса.'                />            }        </li>)};export default SliderThumbnailsItem;