import React from "react";const SliderThumbnailsItem = ({img, onMouseEnter, i, className}) => {    return (        <li className={className} onMouseEnter={() => onMouseEnter(i)}>            {window.innerWidth <= 320 ?                <div>                    <span className='visually-hidden'>{i + 1}</span>                </div>                :                <img                    src={require(img)}                    alt='Фото продукта с другого ракурса.'                />            }        </li>)};export default SliderThumbnailsItem;