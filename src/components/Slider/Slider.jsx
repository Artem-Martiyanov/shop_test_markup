import React, {useState} from 'react';import cl from './Slider.module.css';import SliderPagination from "../SldierPagination/SliderPagination";const Slider = ({product}) => {    const [pictureIndex, setPictureIndex] = useState(0)    const getCurrentPictureIndex = (index) => {        setPictureIndex(index)    }    return (        <div className={cl.wrapper}>            <div className={cl.picture}>                <img src={'products/' + product.images[pictureIndex].pic}                     alt='Фото продукта крупно.'                />            </div>            <SliderPagination images={product.images} current={getCurrentPictureIndex}/>        </div>    );};export default Slider;