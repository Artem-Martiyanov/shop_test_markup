import React, {useState} from 'react';import cl from './Slider.module.css';import SliderPagination from "./SliderPagination";const Slider = ({images}) => {    const [picIndex, setPicIndex] = useState(0)    const [imgClasses, setImgClasses] = useState([cl.img])    const [miniatureClasses, setMiniatureClasses] = useState(images.map((item, index) => {        return index === picIndex ? `${cl.miniature} ${cl.active}` : cl.miniature    }))    const activatePic = (i) => {        setMiniatureClasses(miniatureClasses.map((item, index) => {            return index === i ? `${cl.miniature} ${cl.active}` : cl.miniature        }))    }    const handleMouseEnter = (i) => {        if (i !== picIndex) {            setPicIndex(i)            setImgClasses([cl.img, cl.visible])            setMiniatureClasses(miniatureClasses.map(() => cl.miniature))            activatePic(i)        }    }    return (        <div className={cl.wrapper}>            <div className={cl.picture}>                <img className={imgClasses.join(' ')}                     src={images[picIndex].pic}                     alt='Фото продукта крупно.'                     onAnimationEnd={() => setImgClasses([cl.img])}                />            </div>            <SliderPagination                className={cl.list}                images={images}                onMouseEnter={handleMouseEnter}                classes={miniatureClasses}            />        </div>    );};export default Slider;