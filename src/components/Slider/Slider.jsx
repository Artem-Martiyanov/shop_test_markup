import React, {useState} from 'react';import cl from './Slider.module.css';import SliderPagination from "./SldierPagination/SliderPagination";const Slider = ({images}) => {    const [picIndex, setPicIndex] = useState(0)    const [imgClasses, setImgClasses] = useState([cl.img])    const [picClasses, setPicClasses] = useState(images.map((item, index) => index === picIndex ? `${cl.pagination__item} ${cl.active}` : cl.pagination__item))    const clearPicClasses = () => {        setPicClasses(picClasses.map(() => cl.pagination__item))    }    const activatePic = (i) => {        setPicClasses(picClasses.map((item, index) => index === i ? `${cl.pagination__item} ${cl.active}` : cl.pagination__item))    }    const handleMouseEnter = (i) => {        setPicIndex(i)        setImgClasses([cl.img, cl.visible])        clearPicClasses();        activatePic(i);    }    return (        <div className={cl.wrapper}>            <div className={cl.picture}>                <img className={imgClasses.join(' ')}                     src={images[picIndex].pic}                     alt='Фото продукта крупно.'                     onAnimationEnd={() => setImgClasses([cl.img])}                />            </div>            <SliderPagination                images={images}                onMouseEnter={handleMouseEnter}                classes={picClasses}            />        </div>    );};export default Slider;