import React, {useState} from 'react';import cl from './Slider.module.css';import SliderPagination from "../SldierPagination/SliderPagination";const Slider = ({product}) => {    const [picIndex, setPicIndex] = useState(0)    const [imgClasses, setImgClasses] = useState([cl.img])    const [picture, setPicture] = useState(0)    const getCurrentPictureIndex = (currentIndex) => {        if (picIndex !== currentIndex) {            setImgClasses([...imgClasses, cl.isSwiped])            setPicIndex(currentIndex)        }    }    return (        <div className={cl.wrapper}>            <div className={cl.picture}>                <img className={imgClasses.join(' ')}                     src={'products/' + product.images[picture].pic}                     onAnimationEnd={(event) => {                         setImgClasses([cl.img])                         setPicture(picIndex)                     }}                     alt='Фото продукта крупно.'                />            </div>            <SliderPagination images={product.images} currentPicIndex={getCurrentPictureIndex}/>        </div>    );};export default Slider;