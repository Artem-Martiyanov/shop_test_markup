import SliderThumbnailsItem from "./SliderThumbnailsItem";const SliderThumbnails = ({images, onMouseEnter, thumbClasses, className}) => {    return (        <ul className={className}>            {images.map((img, index) => <SliderThumbnailsItem                onMouseEnter={onMouseEnter}                img={img.thumbnail}                i={index}                key={index}                className={thumbClasses[index]}            />)}        </ul>    );};export default SliderThumbnails;