import React from 'react';import cl from './Features.module.css'const Features = ({features}) => {    return (        <ul className={cl.list}>            {features.map(featureLink => {                return (                    <li className={cl.item} key={featureLink}>                        <img                            className={cl.img}                            src={featureLink}                            alt='Преимущество товара.'/>                    </li>                )            })}        </ul>    );};export default Features;