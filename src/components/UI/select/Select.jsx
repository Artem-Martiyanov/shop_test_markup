import React from 'react';import cl from './Select.module.css'const Select = ({options}) => {    const getOptionsKeys = (optionsObj) => {        const options = []        for (let key in optionsObj) {            options.push(key)        }        return options    }    return (        <select className={cl.select}>            {getOptionsKeys(options).map((size) => <option className={cl.option} value={size} key={size}>Размер: {size}</option>)}        </select>    );};export default Select;