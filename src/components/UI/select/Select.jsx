import React, {useState} from 'react';import cl from './Select.module.css'const Select = ({options, name}) => {        const sizes = Object.keys(options).filter(key => options[key] !== 0)        const [currentOption, setCurrentOption] = useState(sizes[0])        const [selectClasses, setSelectClasses] = useState([cl.select])        const isClicked = () => {            selectClasses.length === 1 ?                setSelectClasses([cl.select, cl.active])                :                setSelectClasses([cl.select])        }        return (            <div className={selectClasses.join(' ')} onClick={() => {                isClicked()            }}            >                Размер: {currentOption}                <svg className={cl.arrow} xmlns='http://www.w3.org/2000/svg' width='12' height='7'>                    <g>                        <path fill='#000'                              d='m6.6 6.7 5.1-5.3c.3-.3.3-.8 0-1.3-.3-.4-.9-.2-1.2 0L6 4.8 1.5.3C1.2 0 .6 0 .3.3.1.3 0 .7 0 .8c0 .2.1.5.3.6l5.1 5.3c.3.4.9.4 1.2 0Z'                        />                    </g>                </svg>                <div className={cl.option__list}>                    {sizes.filter((key => key !== currentOption)).map((key) => {                        return (                            <React.Fragment key={key}>                                <input className={cl.input}                                       onChange={() => console.log(key)}                                       type='radio'                                       id={key}                                       name={name}                                />                                <label className={cl.option}                                       htmlFor={key}                                       onClick={() => setCurrentOption(key)}                                >Размер: {key}</label>                            </React.Fragment>                        )                    })}                </div>            </div>        );    };export default Select;