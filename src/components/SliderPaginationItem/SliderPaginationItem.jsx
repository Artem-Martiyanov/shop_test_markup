import React, {useState} from 'react';import cl from './SliderPaginationItem.module.css'const SliderPaginationItem = ({img, currentPic, i}) => {    const [hover, setHover] = useState(false);    return (        <li className={hover ? cl.item + ' ' + cl.active : cl.item}            onMouseEnter={(event) => {                setHover(true)                currentPic(i)            }}            onMouseLeave={() => {setHover(false)}}>            <img className={cl.img}                 src={'products/' + img}                 alt='Фото продукта с другого ракурса.'/>        </li>    );};export default SliderPaginationItem;